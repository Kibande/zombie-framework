
#include "entities.hpp"
#include "../ntile.hpp"

namespace ntile
{
    /*prop_tree::prop_tree(CFloat3& pos)
    {
        this->pos = pos;

        this->modelPath = "ntile/models/prop_tree";
    }
    
    SERIALIZE_BEGIN(prop_tree, "prop_tree")
        if (output->writeItems<Float3>(&pos, 1) != 1)
            return 0;
        return 1;
    SERIALIZE_END
    
    UNSERIALIZE_BEGIN(prop_tree)
        if (input->readItems<Float3>(&pos, 1) != 1)
            return 0;
        return 1;
    UNSERIALIZE_LINK
        return -1;
    UNSERIALIZE_END*/
}
